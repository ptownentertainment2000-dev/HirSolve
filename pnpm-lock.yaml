git clone https://github.com/ptownentertainment2000-dev/HirSolve.git
cd HirSolve

corepack enable

pnpm install --lockfile-only
git add pnpm-lock.yaml
git commit -m "Add pnpm lockfile"
git push origin main
