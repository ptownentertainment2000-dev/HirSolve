{
  "name": "@hirsolve/auth",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {
    "@hirsolve/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
